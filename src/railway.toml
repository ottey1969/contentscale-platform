[deploy]
# Extend timeout for long AI rewrite calls (default 30s is too short for Claude)
healthcheckTimeout = 300

[environments.production.deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
